name: CI/CD - ClickMarket Location Service

on:
  push:
    branches: [ main ]
    paths:
      - "location_service/**"
      - ".github/workflows/location_service.yml"

  pull_request:
    branches: [ main ]
    paths:
      - "location_service/**"
      - ".github/workflows/location_service.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: location_service

    steps:
      - name: Descargar código del repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Instalar dependencias
        run: npm install

      - name: Compilar y construir servicio
        run: npm run build

  test:
    runs-on: ubuntu-latest
    needs: build
    defaults:
      run:
        working-directory: location_service

    steps:
      - name: Descargar código del repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Instalar dependencias
        run: npm install

      - name: Ejecutar pruebas automatizadas
        run: npm test

  deploy:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    defaults:
      run:
        working-directory: location_service

    steps:
      - name: Descargar código del repositorio
        uses: actions/checkout@v4

      - name: Crear imagen del servicio
        run: |
          echo "Construyendo imagen de Location Service..."
          docker build -t clickmarket/location-service:${{ github.sha }} .

      - name: Simular despliegue Blue-Green
        run: |
          echo "=== BLUE-GREEN DEPLOYMENT ==="
          echo "Versión actual (BLUE): location-service:stable"
          echo "Nueva versión (GREEN): location-service:${{ github.sha }}"
          echo "Desplegando nueva versión en entorno GREEN..."
          echo "Ejecutando health check..."
          echo "Health check exitoso."
          echo "Cambiando tráfico de BLUE a GREEN..."
          echo "Location Service desplegado correctamente."

      - name: Simular rollback
        if: failure()
        run: |
          echo "ERROR: El despliegue de Location Service falló."
          echo "Ejecutando rollback..."
          echo "Restaurando versión BLUE..."
          echo "Rollback completado."
